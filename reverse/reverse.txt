Create class ShopProduct 

Properties 

$title,
$producerMainName;
$producerFirstName;
$price;


methods:

getProducer() to return full names("producer firstname and mainname"),
getSummaryLine() to return("title,producer firstname and mainname")


Create class CdProduct to extends ShopProduct

Properties
parent properties
$playLength;

methods:
getPlayLength() to retunr $playLength
getSummaryLine() to retunr parent  plus playing time


create class BookProduct

Properties
parent properties
$numPages;


methods:
getNumberOfPages()() to retunr $playLength
getSummaryLine() to retunr parent  plus page count



CdProduct and BookProduct must inherit
